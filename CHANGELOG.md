# 更新日志

## 2026-08-21

### CCW 新功能
- **多图片对话输入**：每个会话输入框均支持一次选择多张图片，也可批量拖入或直接粘贴剪贴板图片；发送前支持缩略图预览、单张移除与大图查看。单轮最多 10 张图片，并提供格式、单张大小和总大小校验。
- **语音转文字**：输入框底部新增麦克风按钮，可把语音实时写入当前草稿；支持连续识别、停止录音、识别错误提示，并与键盘输入安全切换。
- **原生工作区选择**：左下角工作区卡片的“更换”按钮接入 Windows 文件夹选择器，选择后同步更新当前会话的项目路径、文件树和 Git 变更。
- **真实停止任务**：Claude Code 运行期间显示独立停止按钮，可取消当前前端请求并终止对应的本地 Claude Code 进程树，停止状态与完成收尾保持一致。
- **运行中排队与转向**：任务执行时可继续输入并加入排队；排队消息支持编辑、恢复到输入框、在侧边会话中打开、删除和关闭排队。选择“调整方向”时会向仍在运行的 Claude Code 会话发送实时控制请求，并明确显示转向状态与结果。
- **模型、批准策略与思考强度**：每个会话可独立选择 Sonnet / Opus / Haiku、五种批准策略，以及快速 / 标准 / 深入 / 极强 / 最大五档思考强度；选择结果真实传入本地 Claude Code，运行中变更从下一轮生效。
- **可折叠左侧栏**：桌面端侧栏现在可以真正收起，并在顶栏提供恢复按钮；支持 `Ctrl+B` 快速切换和本机状态记忆。移动端新增遮罩关闭与 `Esc` 关闭行为。

### CCW 新 UI 与交互
- **Codex 式底部控制区，Claude 配色**：重新组织附件、批准策略、模型与思考强度、语音、发送和停止按钮；保持 Claude 珊瑚橙、暖白/炭黑双主题，同时统一控件高度、圆角、间距与弹出层层级。
- **五档离子思考滑块**：新增带离子流动与渐隐动画的思考强度滑块；五个档位全部位于轨道内，支持点击定位、拖动吸附和明显档位停顿，滑块与下方强度标识同步变化，并适配深浅主题及“减少动态效果”系统设置。
- **Codex 式批准策略面板**：批准选项改为信息层级更清晰的卡片列表，突出当前选择，补充风险说明，并允许弹出层自然溢出输入框而不被裁切。
- **流程展示重构**：Claude 输出过程中显示当前步骤和工具状态；回答完成后自动折叠为轻量摘要，每条回答均可通过“查看流程详情”重新展开完整步骤、工具输入与结果；长流程底部也提供“收起流程”按钮，无需滚回操作区顶部。
- **排队消息卡片**：排队内容改为 Codex 式悬浮卡片和更多菜单，提供“编辑消息 / 在侧边会话中打开 / 关闭排队”等操作，同时保留 Claude 色彩与圆角语言。
- **侧栏开关视觉升级**：原有含义不明确的符号替换为清晰的面板收起/展开图标，增加克制的珊瑚色底、细边框和轻阴影；默认不抢占注意力，悬停时再增强反馈。
- **品牌与可读性调整**：增大主要控制区和正文文字尺度，优化中英文回退字体；更新 Claude 品牌标记、应用图标和高分辨率任务栏图标，改善不同 DPI 下的清晰度。
- **更清爽的整体色温**：浅色主题由偏黄暖白调整为更中性的纸白，深色主题使用中性炭黑；保留 Claude 珊瑚橙作为强调色，并统一网格、边框、阴影与浮层透明度。

### 启动与桌面体验
- 一键启动流程会自动寻找可用端口、启动前端与本地桥接、等待服务就绪，再以 Chrome 应用窗口打开；健康实例会直接复用。
- Windows 启动器与桌面快捷方式改为无控制台方式运行，不再弹出黑色命令窗口；同时保留普通模式和管理员模式入口。
- 提升本地桥接兼容性：支持多图片消息、实时转向、原生目录选择、停止任务、DeepSeek 用量读取与更完整的会话事件收尾。

### 修复
- 修复桌面宽度下“收起侧栏”按钮只关闭移动端状态、实际无法折叠的问题。
- 修复控制区弹出面板被输入框裁切、按钮尺寸不协调、文字过小及多列会话中的层级覆盖问题。
- 修复排队消息更多菜单溢出、转向仅显示提示但未立即送达运行中会话，以及成功插入后新消息未进入 CCW 前端聊天流的问题；现在会按“旧回复 → 插入消息 → 新回复”分段显示，并保留多张图片。
- 修复工作区“更换”按钮无法可靠打开目录选择器，以及选择新目录后部分上下文未刷新的问题。
- 修复停止按钮在运行态缺失或只改变界面状态、没有终止本地任务的问题。

### DeepSeek Harness 扩展
- **dsh-skin v0.1.0**：新增 DeepSeek Harness（dsh web）换肤皮肤包（`@linxin666/dsh-client-ui-skin-claude-code-white`），移植 Claude Code White 的珊瑚橙 × 纸白 × Geist 风格，浅/深两套主题跟随 dsh 切换，只改外观不影响功能。
- 修复 DeepSeek Harness 桌面窗口右上角面板按钮与 Windows 最小化、最大化、关闭按钮重叠；桌面模式自动预留标题栏安全区，普通浏览器布局不受影响。
- 仓库新增 `.vite/` 缓存目录忽略规则。

### 发布
- 重建公开发布包 `claude-code-white-release.zip`（UTF-8 文件名，包含 dsh-skin 与最新文档），同步提交至 GitHub。
- GitHub 主线整理：以 1.0 干净主线替换旧的上传式历史（README 改名、旧 zip 上传等已被取代的提交）。

## 2026-08-14

### 变更
- 桥接鉴权链路（前端换取并携带 `X-Bridge-Token`）合入后**回滚**：保持本地桥接无鉴权的简单架构，仅监听 127.0.0.1。

## 2026-08-13

### 新增
- Claude 任务栏图标（含生成脚本 `scripts/generate-icons.py`）。
- 双语文档（README / README.first）、软件声明。
- 发布脚本升级：`make-release.mjs` 原生实现 ZIP 写入，中文文件名按 UTF-8 编码（修复 Windows bsdtar GBK 乱码）。

### 里程碑
- **Claude Code White 1.0（干净历史基线）**：基于 ef57760 整理。完整功能：工具可视化、按回答自动折叠流程、实时 tokens、记忆读写、一键桥接（同源拉起独立桥接进程）、管理员快捷方式、会话并排/搜索/恢复、用量与额度面板（本地统计 + DeepSeek 平台快照导入）。

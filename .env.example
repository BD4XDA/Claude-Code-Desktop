# Optional provider usage adapters. Copy the values into the environment that
# launches Start-Claude-Code-White.ps1. Keys are read only by the local bridge.
DEEPSEEK_API_KEY=
ANTHROPIC_ADMIN_KEY=
OPENAI_ADMIN_KEY=
GEMINI_API_KEY=
